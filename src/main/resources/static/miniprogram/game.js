// Mini game entry point required by WeChat DevTools when compileType is "game".
// The current gameplay code still lives in the page-style implementation under
// pages/, so this file only satisfies the game bootstrap entry for now.
console.log('PengPengPeng game entry loaded');
